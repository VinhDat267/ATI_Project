# Dependencies and reproducible build output
node_modules/
dist/
.next/
coverage/
generated/
*.tsbuildinfo
.cache/
.turbo/
apps/web/test-results/
apps/web/playwright-report/

# Local environment and credentials; only blank/synthetic templates are shared
.env
.env.*
!.env.example
*.pem
*.key
*.p12
*.pfx
credentials.local.json
secrets.local.json

# Runtime output and local databases (SQL migrations remain versioned)
*.log
*.sqlite
*.sqlite3
*.sqlite-wal
*.sqlite-shm
*.sqlite3-wal
*.sqlite3-shm
*.db
*.db-journal
*.dump
*.backup
/data/
/runtime/
/logs/
/backups/
/tmp/
/temp/

# New archives/backups require explicit review; tracked historical files stay tracked
*.zip
*.7z
*.tar
*.tar.gz
*.bak

# Personal editor and OS state
.idea/
.vscode/
.DS_Store
Thumbs.db
Desktop.ini
.agents/
.claude/
.codex/
.superpowers/
.impeccable/
AGENTS.md
CLAUDE.md

# New evidence captures are ignored by default. After content review, add only
# the exact approved checkpoint files with git add -f -- <path>; never
# force-add a whole directory. See docs/GIT-POLICY.md. Ignore rules do not
# protect already tracked files.
/docs/api-evidence/**/
/docs/task-hub-evidence/**/
/docs/web-evidence/**/

# Codegraph
.codegraph/
.gitnexus/
