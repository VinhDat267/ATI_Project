# FS-03 — Approved filesystem launch and read adapter

**Status:** VERIFIED for the FS-03 read-only checkpoint on 2026-09-14. Filesystem write remains deliberately SPEC_ONLY for FS-04.

## Scope delivered

- Added a trusted `filesystem-local-v1` preset for `@modelcontextprotocol/server-filesystem@2026.8.31` with entry `dist/index.js`, identity `secure-filesystem-server/0.2.0`, policy `b-local-fs-1`, project-runtime principal binding, and a 65,536-byte read limit.
- Recorded the observed raw discovery (14 names), selected raw `read_text_file` and `write_file` schemas, dependency file fingerprints, Node/runtime provenance, and the FS-01 probe source in `config/filesystem-reviewed.json`.
- Added strict artifact/preset verification before launch and before every filesystem call. No runtime download, shell discovery, or plan-supplied executable/root override is used.
- Added the preserving demo-root setup command `npm run fs:demo:setup`. It creates only `runtime/filesystem/<G1_USER_ID>`, `.ati-root.json`, `notes.txt`, and `reports/`; reruns preserve the marker and fixture.
- Added a second MCP connection with no roots capability. Only public `filesystem.read_file` is exposed; the upstream `read_text_file` and `write_file` names stay private. The task_hub catalog remains eight tools, so the enabled catalog is nine tools.
- Added reviewed-output normalization, bounded deadline handling, root-marker revalidation, traversal containment, and fail-closed launch flag handling. `G1_FILESYSTEM_ENABLED` is off by default; invalid values or a missing trusted root return `CONFIG`.
- Updated the CLI and engine README. Read-only inspection commands continue to work without starting MCP.

The setup command was run twice for the demo principal; the marker `root_id` was preserved, the root file matched `.gitignore`, and the owned `runtime/` tree was removed after the check.

## Verification evidence

| Check | Result | Evidence |
|---|---:|---|
| Engine unit suite | 90 passed, 1 skipped | `1789367130840-unit/output.log` |
| Targeted filesystem read integration | 5 passed | `1789367131065-fs-read-integration/output.log` |
| Full `check:engine` | exit 0; DSL 39 passed, engine unit 90 passed/1 skipped, MCP task_hub 56 passed, DB 7 passed, engine integration 45 passed | `1789367141918-final-check/output.log` |

The targeted integration uses two real MCP processes (task_hub and the pinned filesystem server) and an isolated PostgreSQL database/root. It verifies the exact UTF-8 fixture text `Tiến độ ATI\nAPI: Done\n`, nine public tools, hidden `write_file`, traversal rejection before dispatch, and root-marker drift rejection. The adapter unit tests use controlled malformed responses to prove strict normalization and no text-to-JSON fallback.

## Observed raw discovery

The reviewed upstream server exposed these 14 names:

`read_file`, `read_text_file`, `read_media_file`, `read_multiple_files`, `write_file`, `edit_file`, `create_directory`, `list_directory`, `list_directory_with_sizes`, `directory_tree`, `move_file`, `search_files`, `get_file_info`, `list_allowed_directories`.

Only `read_text_file` and `write_file` are selected for schema review. Only the mapped public `read_file` is routable at FS-03.

## Boundary for the next task

This checkpoint does not claim filesystem write success, durable dispatch markers, approval authorization, crash recovery, or post-write reconciliation. Those belong to FS-04. No commit or staging was performed.
