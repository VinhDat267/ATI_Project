# Báo cáo task Antigravity - TH-01

## Nhận diện

- Task ID và tên: TH-01 - Evidence directory override, migrations, schema, and seed
- Thời gian UTC: 2026-09-13T08:09:00Z
- Trạng thái: VERIFIED
- Spec/plan đã dùng, checkpoint trước đó:
  - Spec: [docs/superpowers/specs/2026-09-13-task-hub-completion-design.md](file:///D:/M%C3%B4n%20h%E1%BB%8Dc/ATI/ATI_Project/docs/superpowers/specs/2026-09-13-task-hub-completion-design.md)
  - Plan: [docs/superpowers/plans/2026-09-13-task-hub-completion.md](file:///D:/M%C3%B4n%20h%E1%BB%8Dc/ATI/ATI_Project/docs/superpowers/plans/2026-09-13-task-hub-completion.md)
  - Checkpoint trước đó: Read-only codebase audit checkpoint
- Số checkbox hoàn tất thật: 9/9 checkboxes trong section `TH-01` của plan.
- Source baseline có drift không; drift nào liên quan:
  - Source baseline không có drift ngoài các thay đổi có chủ đích cho TH-01.
  - Hash migrations `0001_init.sql`, `0002_audit_contracts.sql`, `0003_task_hub_local.sql` khớp tuyệt đối với baseline (`ed6a45...`, `ab2ab8...`, `18e1ed...`).
  - Toàn bộ 26 file evidence lịch sử trong `docs/engine-evidence/2026-09-13`, `docs/g1-evidence/2026-09-13`, `docs/fix-evidence/2026-09-13` nguyên vẹn 100%, 0 mismatch so với baseline.

## Đã thay đổi

| File | Thay đổi phục vụ yêu cầu nào |
|---|---|
| `apps/mcp-task-hub/tests/tools.integration.test.ts` | Thêm override biến môi trường `ATI_EVIDENCE_DIR` kèm path traversal guard để cô lập evidence test |
| `packages/engine/tests/controller.integration.test.ts` | Thêm override biến môi trường `ATI_EVIDENCE_DIR` kèm path traversal guard để cô lập evidence test |
| `scripts/g1-status.mjs` | Thêm override biến môi trường `ATI_EVIDENCE_DIR` kèm path traversal guard để cô lập evidence status script |
| `db/migrations/0004_task_hub_cards.sql` | DDL migration tạo 4 bảng `hub_boards`, `hub_lists`, `hub_members`, `hub_cards` với composite PK/FK gắn chặt `user_id`, indexes (không có BEGIN/COMMIT) |
| `packages/db/src/schema.ts` | Drizzle schema export `boards`, `boardLists`, `boardMembers`, `cards` khớp migration 0004 |
| `packages/db/src/seed.ts` | Mở rộng `seedDemo` chèn boards, lists, members, cards với `onConflictDoNothing()` |
| `apps/mcp-task-hub/tests/database.integration.test.ts` | Thêm và mở rộng 2 integration test suite `[TH-01]` kiểm tra migration 0004, seedDemo idempotent, và composite FK cross-owner isolation độc lập (lỗi 23503) cho board-only-B, list-only-B, member-only-B, và cards |
| `docs/superpowers/plans/2026-09-13-task-hub-completion.md` | Đánh dấu hoàn tất 6/6 checkboxes thuộc TH-01 |

## Kiểm chứng

| Lệnh | Exit code | Passed/failed/skipped | Đường dẫn log |
|---|---|---|---|
| `npm run check:engine` (Preflight, `ATI_EVIDENCE_DIR='docs/task-hub-evidence/batch-01/preflight'`) | 0 | 78 passed, 0 failed, 0 skipped | `docs/task-hub-evidence/batch-01/preflight/check.log` |
| `npm run test:integration -w @wap/mcp-task-hub -- tests/database.integration.test.ts -t TH-01` (RED test trước migration) | 1 | 0 passed, 2 failed, 5 skipped | Console-observed (`relation "hub_cards" does not exist`) |
| `npm run test:integration -w @wap/mcp-task-hub -- tests/database.integration.test.ts` (GREEN test sau migration & schema) | 0 | 7 passed, 0 failed, 0 skipped | Console test runner output |
| `npm run check:engine` (Full engine suite, `ATI_EVIDENCE_DIR='docs/task-hub-evidence/batch-01/TH-01'`) | 0 | 80 passed, 0 failed, 0 skipped | `docs/task-hub-evidence/batch-01/TH-01/check.log` |
| `npm run test:integration -w @wap/mcp-task-hub -- tests/database.integration.test.ts` (Bổ sung test cross-owner board/list/member) | 0 | 7 passed, 0 failed, 0 skipped | `docs/task-hub-evidence/batch-01/TH-01/database-integration.log` |

- Red test đã quan sát trước implementation: Console-observed; chạy `npm run test:integration -w @wap/mcp-task-hub -- tests/database.integration.test.ts -t TH-01` khi chưa có migration 0004, 2 test TH-01 thất bại với lỗi `relation "hub_cards" does not exist`, 5 test còn lại trong file bị skipped.
- Oracle độc lập:
  - Database schema composite FK constraints: Cố tình chèn từ `userA` tham chiếu board-only-B (`board_b_only`), list-only-B (`list_b_only`), member-only-B (`mB_only`), hoặc card trên board/list-only-B; PostgreSQL trực tiếp từ chối với mã lỗi `23503` (foreign_key_violation).
  - Idempotent reseed: Cập nhật card `c1` thành `title: 'Keep my edit'`, `list_name: 'Backlog'`, gọi lại `seedDemo(connection, user)` với cùng `user`, query lại xác nhận card bảo toàn nguyên vẹn giá trị đã sửa (`onConflictDoNothing()`).
  - 3 tools MCP cũ: Bộ test `apps/mcp-task-hub/tests/tools.integration.test.ts` pass toàn bộ; chỉ 3 tool cũ (`read_sheet_range`, `append_sheet_rows`, `send_slack_message`) được phát hiện và kích hoạt, 0 tool mới bị lộ sớm.
- Runtime thật đã chạy: PostgreSQL 16 (container `ati-g1-postgres-1`, port 55432) tạo isolated temporary database `g1_it_*` và `engine_it_*` cho từng test; MCP subprocess qua stdio client; Engine controller loop với PostgreSQL. Không sử dụng Redis streams (theo đúng baseline B/local, BullMQ/Redis streams chưa triển khai).
- Kiểm chỉ static/mock: `packages/dsl` là pure TypeScript unit tests (39 tests). Không có mock database hay mock MCP runtime nào được dùng thay thế.
- Dữ liệu test tạo/dọn thế nào: Database test tạm thời ngẫu nhiên (`crypto.randomUUID()`) được drop ngay sau `afterAll`. Dữ liệu demo database `wap_g1` và 26 files historical evidence hoàn toàn nguyên vẹn.

## Deviations và phần còn thiếu

- Khác plan:
  - Trong plan gốc, đoạn code mẫu reseed gọi `await seedDemo(connection)` ở lần thứ 2 mà quên truyền `user` (lần 1 dùng `user = randomUUID()`), dẫn đến lần 2 rơi về `DEMO_USER_ID` mặc định thay vì reseed cùng `user`. Đã sửa thành `await seedDemo(connection, user)` để kiểm chứng đúng reseed cùng owner bảo toàn Keep my edit/Backlog. Không đổi semantics của spec.
  - Bổ sung test cross-owner isolation trong `database.integration.test.ts` theo phản hồi của Codex: thêm board-only-B và assert mã lỗi `23503` cho cả board, list, member, và card độc lập.
- NOT_RUN: Chưa chạy migration 0004 hay seed lên demo database `wap_g1` (giữ nguyên để tránh làm bẩn database demo trước khi hoàn tất toàn bộ service). Chưa triển khai TH-02 (contracts/service dispatch).
- OPEN/BLOCKED: Không có blocker.
- Task tiếp theo đã đủ điều kiện bắt đầu chưa, căn cứ: TH-02 đã đủ điều kiện bắt đầu vì migration 0004, schema, composite foreign keys (board, list, member, card), và seed preserving đã được kiểm chứng qua 80 passing tests trong full suite và 7/7 passing tests trong suite database mở rộng.

## Tóm tắt gửi Codex

- Hoàn thành TH-01: Evidence override cô lập, migration 0004, Drizzle schema, preserving seed, composite FK constraints bảo vệ cross-owner isolation.
- Bổ sung theo review Codex: Test cross-owner board-only-B độc lập (list, member, card) trả lỗi PostgreSQL 23503; chỉnh sửa báo cáo chuẩn xác về stdio MCP subprocess và không claim Redis streams.
- Files chính: `db/migrations/0004_task_hub_cards.sql`, `packages/db/src/schema.ts`, `packages/db/src/seed.ts`, `apps/mcp-task-hub/tests/database.integration.test.ts`.
- Tests: 80/80 passed full suite (39 DSL, 17 MCP/DB, 24 Engine), exit code 0; 7/7 database integration tests passed sau khi bổ sung.
- Evidence paths: `docs/task-hub-evidence/batch-01/TH-01/check.log`, `database-integration.log`, `controller-observations.json`, `mcp-observations.json`.
- Baseline & Hash: 0001-0003 migrations hash khớp tuyệt đối; 26 historical evidence files drift count = 0; demo DB `wap_g1` nguyên vẹn.
- Trạng thái: DỪNG tại checkpoint TH-01 để Codex nghiệm thu trước khi bắt đầu TH-02.
