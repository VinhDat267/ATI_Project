# Báo cáo task Antigravity - TH-02

## Nhận diện

- Task ID và tên: TH-02 - Explicit contracts and dispatch boundary
- Thời gian UTC: 2026-09-13T08:17:00Z
- Trạng thái: VERIFIED
- Spec/plan đã dùng, checkpoint trước đó:
  - Spec: [docs/superpowers/specs/2026-09-13-task-hub-completion-design.md](file:///D:/M%C3%B4n%20h%E1%BB%8Dc/ATI/ATI_Project/docs/superpowers/specs/2026-09-13-task-hub-completion-design.md)
  - Plan: [docs/superpowers/plans/2026-09-13-task-hub-completion.md](file:///D:/M%C3%B4n%20h%E1%BB%8Dc/ATI/ATI_Project/docs/superpowers/plans/2026-09-13-task-hub-completion.md)
  - Checkpoint trước đó: TH-01 checkpoint (9/9 checkboxes verified)
- Số checkbox hoàn tất thật: 5/5 checkboxes trong section `TH-02` của plan.
- Source baseline có drift không; drift nào liên quan:
  - Source baseline không có drift ngoài các thay đổi có chủ đích cho TH-01 và TH-02.
  - Toàn bộ 26 file evidence lịch sử trong `docs/engine-evidence/2026-09-13`, `docs/g1-evidence/2026-09-13`, `docs/fix-evidence/2026-09-13` nguyên vẹn 100% (0 mismatch so với baseline).
  - Migrations `0001`–`0003` hash khớp tuyệt đối baseline. Demo DB `wap_g1` nguyên vẹn.

## Đã thay đổi

| File | Thay đổi phục vụ yêu cầu nào |
|---|---|
| `apps/mcp-task-hub/src/errors.ts` | Tạo mới file tách riêng class `ToolError` dùng chung, tránh circular dependency |
| `apps/mcp-task-hub/src/contracts.ts` | Mở rộng union đủ 8 `ToolName`, định nghĩa `ENABLED_TOOL_NAMES` (3 tool cũ), `EnabledToolNameSchema`, strict inputs/outputs cho 5 tool mới, readOnlyHint/destructiveHint annotations, và `toolDefinitions()` duyệt qua `ENABLED_TOOL_NAMES` |
| `apps/mcp-task-hub/src/service.ts` | Import và re-export `ToolError` từ `./errors.js`; thay generic `else` write bằng `else if (name === "send_slack_message")` và throw `ToolError("BAD_ARGS", "Tool is not enabled")` ở nhánh cuối |
| `apps/mcp-task-hub/src/server.ts` | Dùng `EnabledToolNameSchema` để parse `request.params.name` khi nhận request qua MCP |
| `apps/mcp-task-hub/tests/tools.integration.test.ts` | Thêm 2 test suite `[TH-02]`: kiểm tra strict schemas cho 5 tool mới và kiểm tra bền vững `create_card` không authorization qua MCP trả `isError: true`, không làm biến đổi `hub_cards` và không thêm `hub_receipts` |
| `docs/antigravity/README.md` | Bổ sung hướng dẫn khảo sát toàn repo đầu mỗi conversation theo yêu cầu mới |
| `docs/superpowers/plans/2026-09-13-task-hub-completion.md` | Đánh dấu hoàn tất 5/5 checkboxes thuộc TH-02 |

## Kiểm chứng

| Lệnh | Exit code | Passed/failed/skipped | Đường dẫn log |
|---|---|---|---|
| `npm run test:integration -w @wap/mcp-task-hub -- tests/tools.integration.test.ts -t TH-02` (RED test) | 1 | 1 passed, 1 failed, 10 skipped | `docs/task-hub-evidence/batch-01/TH-02/red-test.log` |
| `npm run test:integration -w @wap/mcp-task-hub -- tests/tools.integration.test.ts -t TH-02` (GREEN test) | 0 | 2 passed, 0 failed, 10 skipped | `docs/task-hub-evidence/batch-01/TH-02/green-test.log` |
| `npm run test:integration -w @wap/mcp-task-hub -- tests/tools.integration.test.ts` (Full tools integration suite) | 0 | 12 passed, 0 failed, 0 skipped | `docs/task-hub-evidence/batch-01/TH-02/tools-integration.log` |
| `npm run check:engine` (`ATI_EVIDENCE_DIR='docs/task-hub-evidence/batch-01/TH-02'`) | 0 | 82 passed, 0 failed, 0 skipped (39 DSL, 19 MCP/DB, 24 Engine) | `docs/task-hub-evidence/batch-01/TH-02/check.log` |

- Red test đã quan sát trước implementation:
  - Lệnh: `npm run test:integration -w @wap/mcp-task-hub -- tests/tools.integration.test.ts -t TH-02`
  - Lỗi quan sát: `TypeError: Cannot read properties of undefined (reading 'safeParse')` tại `tests/tools.integration.test.ts:480:33` do `inputs.create_card` chưa được định nghĩa; ghi nhận chi tiết tại `red-test.log`.
- Oracle độc lập:
  - MCP Live Discovery: Test `discovers exactly the implemented tools with output schemas, then reads an A1 range` xác nhận live server MCP stdio chỉ công bố chính xác 3 tool cũ (`read_sheet_range`, `append_sheet_rows`, `send_slack_message`), 0 tool mới bị lộ ra ngoài.
  - Zero-mutation Oracle: Test `create_card without authorization cannot mutate hub_cards or add receipts` gọi MCP trực tiếp; SQL query `SELECT count(*)::int` trước và sau call xác nhận: `hub_cards` count không đổi (0 mutation), `hub_receipts` count không đổi (0 receipt).
  - Schema Constraints: Zod schemas từ chối title toàn khoảng trắng `{"title": "  "}`, từ chối ngày không tồn tại `{"due_date": "2026-02-30"}`, từ chối spoofed caller-supplied `user_id` và extra output fields.
- Runtime thật đã chạy:
  - PostgreSQL 16 (container `ati-g1-postgres-1`, port 55432) tạo isolated temporary database `g1_it_*` và `engine_it_*` cho các integration suite;
  - MCP subprocess chạy qua StdioClientTransport / process Node.js thật gọi server stdio;
  - Engine controller execution loop chạy độc lập với 24 tests. Không sử dụng Redis streams.
- Kiểm chỉ static/mock: `packages/dsl` là pure TypeScript unit tests (39 tests). Không có mock runtime hay mock database nào được dùng thay thế.
- Dữ liệu test tạo/dọn thế nào: Database test tạm thời ngẫu nhiên (`crypto.randomUUID()`) được drop ngay sau `afterAll`. Dữ liệu demo database `wap_g1` và 26 files historical evidence hoàn toàn nguyên vẹn.

## Deviations và phần còn thiếu

- Khác plan: Không có. Giữ nguyên gate/receipt và emitted schemas của 3 tool cũ; chưa thêm `cards.ts`, chưa bật tool mới, không promotion catalog evidence.
- NOT_RUN: Chưa triển khai 5 tool handlers trong `cards.ts` (thuộc TH-03, TH-04, TH-05); chưa bật 5 tool mới trong `ENABLED_TOOL_NAMES`; chưa migrate/seed demo DB `wap_g1`.
- OPEN/BLOCKED: Không có blocker.
- Task tiếp theo đã đủ điều kiện bắt đầu chưa, căn cứ: TH-03 đã đủ điều kiện bắt đầu vì strict contracts cho 5 tool mới, explicit dispatch boundary, shared `ToolError`, và cơ chế kiểm soát publication qua `EnabledToolNameSchema` đã được kiểm chứng qua 82 passing tests (39 DSL, 19 MCP/DB, 24 Engine).

## Tóm tắt gửi Codex

- Hoàn thành TH-02: Strict contracts đủ 8 tool names, `ENABLED_TOOL_NAMES` cố định 3 tool cũ, explicit dispatch boundary, `ToolError` tách sang `errors.ts`.
- Bổ sung tài liệu: Đã thêm quy định khảo sát đầu conversation vào `docs/antigravity/README.md`.
- Files chính: `apps/mcp-task-hub/src/contracts.ts`, `errors.ts`, `service.ts`, `server.ts`, `tests/tools.integration.test.ts`.
- Tests: RED test quan sát lỗi thiếu `inputs.create_card`; GREEN đạt 82/82 passed full suite (39 DSL, 19 MCP/DB, 24 Engine), exit code 0.
- Evidence paths: `docs/task-hub-evidence/batch-01/TH-02/check.log`, `red-test.log`, `green-test.log`, `tools-integration.log`.
- Invariants: Live discovery chỉ công bố 3 tool cũ; `create_card` không authorization trả `isError: true` và zero mutation/receipt; 26 historical evidence files 0 drift; demo DB `wap_g1` nguyên vẹn.
- Trạng thái: Đã đánh dấu 5/5 checkboxes trong plan; **DỪNG tại checkpoint TH-02** để Codex review trước khi nhận TH-03.
