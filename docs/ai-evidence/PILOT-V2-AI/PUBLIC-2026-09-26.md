# Gemini public evaluation after input guidance fixes

Status: `HISTORICAL_PUBLIC_26_OF_26_PLANNER_RESULT_PASS / OFFLINE_PUBLIC_STRICT_18_PASS_8_FAIL / PUBLIC_SEMANTIC_FINDINGS_OPEN / HOLDOUT_NOT_RUN / HANDOFF_BLOCKED`.

The 26/26 result below is historical PlannerResult-level grading of the original campaign. The first [strict offline full-DSL regrade](FULL-DSL-REGRADING-2026-09-26.md) reported 18 PASS / 8 FAIL, but a later targeted smoke found a catalog-to-DSL identity mismatch in that evaluator. The corrected offline regrade of the same 26 saved observations is **19 PASS, 7 FAIL**: seven of eight plans fail, while V2-04-en passes. The earlier strict counts are superseded. No new provider call or holdout access occurred during either regrade.

## Frozen execution

- Commit: `d35a9af68daffc50c3b8b44cbdc16e69e351427c`.
- Model: `google / gemini-3.1-flash-lite / interactions`, fixed pilot catalog, `model-only-v1`.
- Freeze: `9363dc2b9778530729899e9901ca09cfaa30f8e6fef413b655c321e968d6c2f8`.
- Manifest: `%TEMP%\pilot-public-inputs-manifest-20260926-1306.json`.
- Journal: `%TEMP%\pilot-public-inputs-journal-20260926-1306`.
- Report: `%TEMP%\pilot-public-inputs-report-20260926-1306.json`.
- Public: 26/26 observations, 26 historical PlannerResult-level automatic PASS (13 Vietnamese, 13 English), 0 failed provider attempts, 0 unsafe reasons, 0 remote effects. Holdout: 20 unattempted.
- Public latency: median 3,354.7 ms; p95 5,272.5 ms. Reported public usage: 54,962 input tokens, 7,720 output tokens.
- Campaign cap: 46 calls, 0 USD, current exact-model Free Tier price evidence, owner attestation of billing disabled and matching credential identity. No paid fallback, blind retry or Trello dispatch. Actual invoice has not been reconciled.

The aggregate report is still `PARTIAL_NOT_MEASURED` because holdout is unattempted. Its 20 grade failures/missing-usage entries are placeholders for those unattempted cases, not provider failures. Automatic grading covers decision kind, proposed write count, reviewed tool and exact arguments where specified; it does not certify every factual statement, semantic/QE comparison or product API integration.

## Confirmed input failures and changes

1. A public campaign at `b4a5a3b` stopped after 9 calls at V2-03-vi with `output_wire_dsl_plan_inputs` (8 automatic PASS, 1 failed, 37 unattempted). Manifest/report: `%TEMP%\pilot-public-manifest-20260926-125310.json` and `%TEMP%\pilot-public-report-20260926-125310.json`.
2. Fixed-category diagnostics at `29bf754` distinguished invalid input names, required/default conflict, default type and other input fields. A one-call V2-03-vi diagnostic passed; a separate V2-03-en diagnostic confirmed `output_wire_dsl_input_key`. Raw invalid field values were not retained; camelCase in fixtures is a representative invalid name, not a recovered provider payload.
3. `8677864` added input-name guidance matching the DSL regex and distinguished input variables from tool argument keys. Its public campaign stopped after 9 calls at V2-03-vi with `output_wire_dsl_input_required_default`.
4. `d35a9af` added explicit required/default exclusion and scalar default type guidance in the wire input schema and pilot system prompt. Both V2-03 languages and all other eligible public cases completed and passed automatic grading.

Google documents `description` as schema guidance and recommends application validation ([structured outputs](https://ai.google.dev/gemini-api/docs/structured-output)). The patch uses supported descriptions and prompt rules; it does not assume unsupported regex grammar enforcement. Runtime DSL validation remains strict. Invalid keys are not renamed and conflicting defaults are not silently removed.

This confirms the two observed failure classes and a successful public run after fixes. It does not prove that Gemini will always conform or retrospectively establish the exact subtype of the earliest coarse V2-03-vi failure.

## Verification and independent review

- Tests were added before the corresponding changes and observed failing. Codec fixtures reject invalid names and required+default declarations, accept valid counterparts, and still reject default type mismatch.
- Targeted provider/prompt/freeze/evaluation: 72 PASS. Diagnostic/journal tests: 44 PASS, including fixed categories persisted across reopening and canary secret exclusion.
- `npm run check`: PASS; DSL 44, engine 615 PASS + 1 skipped, API 86, web 139; typecheck/build/schema/OpenAPI succeeded. Existing web bundle-size warning remains.
- Independent Code Reviewer found no blocker in the bounded guidance changes, then separately reviewed all 26 public observations against the public dataset. It ran full `WorkflowPlanSchema.safeParse` on the eight observed plans. No holdout oracle was opened by the implementing agent or reviewer.

## Independent public semantic findings

All 26 branch decisions match the public expectations. Reviewed tool arguments in all eight plans match the source/card IDs, board/list, dates and source business fields. No write was proposed for read-only, clarification or refusal cases. However, the historical automatic grader validated a `PlannerResult` draft and selected effects; that result does not establish executable full DSL acceptance. The corrected linked offline regrade applies the full-DSL gate: seven saved plans fail, and one passes structural validation.

| Finding | Public variants | Evidence and implication |
|---|---|---|
| Missing write-step idempotency key | V2-01-vi/en, V2-02-en | Full `WorkflowPlanSchema` rejects all three. A draft plan must not be presented as ready for dispatch. |
| Literal prose outputs instead of references | V2-02-vi, V2-03-vi | Full `WorkflowPlanSchema` rejects both. Success wording in proposed outputs is not evidence of completed SaaS effects. |
| Unproven output references | V2-03-en, V2-04-vi | One omits the `steps` root; one uses `result` rather than the documented `output` path. Runtime resolution still needs a targeted check. |
| A4 dimensions incorrectly treated as missing | V2-05-vi/en | A4 is present in selected source, but checklist dimension recognition handles numeric units/aspect ratios. This is a source/checklist issue, not evidence that Gemini independently invented a missing fact. |
| Clarification allows weaker assignee identifiers | V2-10-vi/en | Questions allow username/clear identifier as alternatives to verified exact member ID. Both still return clarification with zero proposed writes. |

Thus **5/8 observed plans fail full workflow schema**; two more have unresolved output references. After correcting the evaluator's tool identity mapping, V2-04-en passes structural validation, leaving **7/8 plan observations failing**. A4 and assignment wording remain separately attributed source/checklist and clarification/prompt findings; structural PASS for those non-plan cases does not settle them. The observed input failures have been addressed and did not recur in this historical 26-case run, but full AI Quality acceptance remains blocked. Full DSL/reference acceptance is now in the grader; a new public freeze is needed to verify the updated planner guidance and A4/member-ID changes before any holdout run. Source-aware API wiring and user UI acceptance remain separate gates.

The reviewed scope remains the separate provider evaluation runner. Source-aware AI is not yet fully connected to the product pilot API; owner UI UC1/UC2/UC3 acceptance and full handoff gates remain open.
