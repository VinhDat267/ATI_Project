# Pilot v2 Gemini Free Tier — điều tra 503 và probe model thay thế

**Trạng thái cập nhật 26/09/2026:** Gemini 3.1 Flash-Lite đã trả observation và usage; hai ca trước sửa dataset gate đạt chấm cấu trúc. `AI_QUALITY_NOT_MEASURED` vẫn giữ: bộ đo chưa hoàn tất và chưa adjudicate nội dung.

## Bằng chứng

- Sau khi sửa request Interactions và ghi diagnostic an toàn tại commit `f4ceed7`, một chiến dịch Gemini `gemini-3.7-flash` mới vẫn nhận HTTP 503, mã provider `service_unavailable`, `Retry-After: 30s`. Journal ghi 1 attempt failed, usage unknown; report `PARTIAL_NOT_MEASURED` với 59/60 ca chưa chạy. Đây là lỗi phía provider theo [bảng lỗi Google](https://ai.google.dev/gemini-api/docs/api-errors); không có bằng chứng rằng sửa giao thức đã giải quyết được khả năng gọi inference.
- Commit `4691c3d` thêm lựa chọn tường minh `gemini-3.8-flash`. CLI đọc [bảng giá Google](https://ai.google.dev/gemini-api/docs/pricing?hl=en), xác nhận đúng mục Standard Free Tier của model đó, rồi khóa model, key fingerprint, Git commit và trần `0 USD` trong manifest. Reviewer độc lập không thấy lỗi chặn trong cổng lựa chọn/khóa model. Test pricing 4/4 và syntax check đạt.
- Manifest cục bộ `%TEMP%\pilot-quality-manifest-20260926-071641.json`, freeze hash `12905e3d25b1cafc12a334cec83600d61e8503e96f716d8bc59cabc96ed06277`; journal `%TEMP%\pilot-quality-journal-20260926-071641\`; report `%TEMP%\pilot-quality-report-20260926-071641.json`. Những artifact này nằm ngoài Git. Chiến dịch dùng commit `4691c3d`, `fixed-catalog`, tối đa 60 calls và 0 USD theo attestation Free Tier của chủ project; chưa đối soát hóa đơn.
- Một probe `gemini-3.8-flash` nhận HTTP 429, `Retry-After: 14s`; provider code/status không có trong diagnostic đã lưu. Journal ghi 1 attempt failed, usage unknown; report `PARTIAL_NOT_MEASURED`, attempted 1/60, failedAttempts 1, unattempted 59. Không suy đoán 429 là hạn mức phút hay ngày. Google liệt kê cả rate limit ngắn hạn và daily quota cho 429; [hạn mức áp dụng theo project](https://ai.google.dev/gemini-api/docs/rate-limits), không theo từng key.
- Không tự retry, không chuyển key/account để vượt hạn mức, không có lời gọi Trello/Sheets từ runner. Không có observation để chấm chất lượng, latency phân phối hay tổng token. `gradeFailuresIncludingUnattempted: 60` không có nghĩa 60 lời gọi provider thất bại.

## Ảnh Rate Limit và probe tiếp theo

- Chủ project gửi ảnh Google AI Studio cho project hiển thị là `AI Automation`, badge `Free tier`, thời gian `Last Hour`, rồi xác nhận `GEMINI_API_KEY` thuộc đúng project này mà không tiết lộ key. Hàng `Gemini 3.8 Flash` cho thấy peak RPM `1/5`, TPM `856/250K`, RPD `1/20` tại thời điểm chụp. Đây là xác nhận của chủ project về danh tính key; số liệu usage có thể trễ và hạn mức công bố không bảo đảm capacity tức thời. Không diễn giải ảnh là bằng chứng chắc chắn rằng một trong ba quota đã bị vượt.
- Từ Git HEAD sạch `070b777`, tạo manifest mới `%TEMP%\pilot-quality-manifest-20260926-072157.json` cho `gemini-3.8-flash`, Free Tier/0 USD, freeze hash `1da11d0e55705f85a3765c024bb191e1cdd472f7d981fb49c9b9ec0edc26bedc`. Probe duy nhất tiếp tục nhận HTTP 429, lần này `Retry-After: 49s`; diagnostic vẫn không có provider code/status. Journal `%TEMP%\pilot-quality-journal-20260926-072157\` và report `%TEMP%\pilot-quality-report-20260926-072157.json` ghi 1 failed attempt, 59 unattempted, usage unknown, `PARTIAL_NOT_MEASURED`. Không chạy lại trên journal này.
- Với RPD 20 như ảnh, bộ 60 ca không thể hoàn tất trong một ngày trên cùng project ngay cả khi provider hoạt động bình thường. Manifest hiện hết hạn attestation sau 6 giờ, vì vậy runner hiện tại cần thiết kế một cách đo nhiều ngày có khóa cùng code/dataset/model và xác nhận Free Tier mới cho mỗi ngày trước khi chạy toàn bộ bộ ca. Chưa triển khai thay đổi này khi probe đầu tiên còn 429.
- Bộ lọc diagnostic trước đó thiếu mã `too_many_requests` trong danh sách 429 hợp lệ mà Google công bố. Đã thêm mã này với test chứng minh chỉ giữ mã và `Retry-After`, không giữ thông điệp provider. Test provider clients đạt 23/23, engine unit đạt 571 passed, 1 skipped; build engine đạt. Đây chỉ là cải thiện diagnostic cho lời gọi sau, **không xác định lại** nguyên nhân của hai phản hồi 429 đã xảy ra.

## Cổng tiếp theo

Tạo manifest mới sau [sửa scope/dataset gate](MODEL-SCOPE-2026-09-26.md); bộ mới 46 ca, không dùng holdout legacy. Model `gemini-3.1-flash-lite` được chọn tường minh và kiểm giá Free Tier riêng. Không suy ra Gemini 3.7/3.8 đã hết 503/429 từ thành công của model này.

## Flash-Lite trước sửa scope

- Tại commit `0bea86a`, model `gemini-3.1-flash-lite` gọi thành công nhưng probe sai board/thiếu args: prompt chưa có trusted board/list và planning args. Không tính probe đó là quality pass.
- Commit `28fdb3e` bổ sung context từ policy/catalog đã review. Manifest `%TEMP%\pilot-quality-manifest-20260926-074541.json`, freeze `9c9db064ad330bce2ec7770f6a72f5b6f5a410de9e3c016ec5d22058d33c1b91`, journal `%TEMP%\pilot-quality-journal-20260926-074541\`.
- Probe V2-01-vi đạt structural PASS (1620 input/704 output tokens, khoảng 4142 ms). Smoke gọi thêm V2-05-vi và đạt structural PASS, rồi dừng trước reserve/provider ở V2-11-vi vì parser `NOT_FOUND` chưa được runner biểu diễn. Report `%TEMP%\pilot-quality-report-smoke-20260926-074541.json` ghi 2 attempted, 2 passed, 58 unattempted; không có remote effects. Usage của các attempt có số token; usage toàn bộ bộ chưa chạy vẫn unknown.
- Hai attempt này không được gộp vào mẫu mới vì code/profile/freeze đã đổi. Lỗi source và oracle được xử lý ở scope mới; không sửa điểm của chiến dịch cũ.
