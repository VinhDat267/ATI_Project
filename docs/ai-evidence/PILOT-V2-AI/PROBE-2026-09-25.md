# Pilot v2 Gemini Free Tier — kết quả probe thật

**Trạng thái:** `PROVIDER_PROBE_FAILED / AI_QUALITY_NOT_MEASURED` lúc 25/09/2026. Không chạy smoke, public hay holdout.

- Commit chiến dịch: `634de9f`; model khóa: `gemini-3.7-flash`; đường đánh giá: `fixed-catalog`; trần khai báo 60 lời gọi, 0 USD. Chủ project xác nhận project chứa key ở Free Tier và chưa liên kết billing. CLI đối chiếu đúng mục model trên trang giá Gemini trước khi tạo manifest. Đây là xác nhận tier của chủ project, chưa phải đối soát hóa đơn.
- Manifest cục bộ ngoài Git: `%TEMP%\pilot-quality-manifest-20260925-134519.json`; freeze hash `6d5280a38bdf3d9d60c9d4131abd71369ef056fdff8c8ddbb6a3a2579059212b`. Journal cục bộ ngoài Git: `%TEMP%\pilot-quality-journal-20260925-134519\`; report: `%TEMP%\pilot-quality-report-20260925-134519.json`. Các file có định danh/quan sát và không đưa vào repo.
- Một attempt public được journal ghi `reserved` rồi `failed`, cùng attempt ID `39b354f9-80f3-49aa-bd4d-9a27293583d1`. Provider trả HTTP 503 sau khoảng 13 giây; usage `unknown`, không có observation để chấm. Không tự retry, không đổi key/account và không gửi lời gọi provider khác. 59 ca còn lại **chưa chạy**.
- CLI report ghi `PARTIAL_NOT_MEASURED`, `attempted: 1/60`, `missingUsage: 60`. Trường `failed: 60` trong grader hiện gồm cả 59 ca thiếu observation; **không phải** 60 lời gọi thất bại. Không suy ra accuracy, latency phân phối, token tổng hay chi phí thực tế từ report này. Free Tier 0 USD là điều kiện giá/tier đã xác nhận, không phải hóa đơn đã kiểm.
- Trello write flag vẫn tắt; runner quality không gọi Sheets/Trello. Card sandbox đã có trước probe không phải kết quả từ Gemini.

**Cổng tiếp theo:** xác định tình trạng Gemini 503 và kiểm lại khả năng gọi model/Interactions API bằng một chiến dịch mới được khóa và review; không tái sử dụng attempt có outcome không chắc chắn. Chỉ chấm chất lượng sau khi có observation bền vững và người chấm độc lập xem nội dung. Nghiệm thu trực tiếp của chủ project với card được ghi riêng tại `docs/PILOT-V2-USER-ACCEPTANCE.md`; UC1/UC2 UI/UC3 chưa hoàn tất.
