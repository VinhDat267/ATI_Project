# API status — 2026-09-15

`API_TECHNICAL_PASS` for the local `DEV_FIXTURE_PLANNER` path after closing API-GATE on 2026-09-17. The adversarial audit remains the historical reason the earlier unsupported full-pass claim was withdrawn; the replacement verdict is backed by the explicit H01–H20 matrix and independent cleanup delta in the [fresh gate manifest](api-evidence/batch-03/API-05/20260917072147-30545e43-9414-43c8-9386-7fed97acc4e0/manifest.json). Fix verification history is tracked in [API audit fix progress](api-evidence/batch-03/API-AUDIT-FIX-PROGRESS.md).

Confirmed on PostgreSQL 16 with real Node HTTP sockets and local MCP connections:

- Login/session, durable `202` acceptance, async planning, owned history/detail, event polling and stable trace cursors.
- Strict approval/cancel, double-decision conflict, server-clock expiry, execute outbox dispatch and startup orphan recovery.
- Positive task_hub flow (`b02`) and two-server filesystem + task_hub flow (`fs-copy-notify`) with no receiver mutation before approval.
- Sanitized acceptance evidence generated by `node scripts/check-api.mjs` under `docs/api-evidence/batch-03/API-05/<run>/`.
- H01–H20 all `PASS`; runner/check/API unit/API integration/task-hub integration/engine integration all exited `0`.
- Cleanup oracle observed no new owned PostgreSQL database, temp root or project process after any of the six gate commands. The oracle is delta-based and deliberately preserves residue that existed before the run.

The planner in these checks is `DEV_FIXTURE_PLANNER`; this is lifecycle evidence, not AI quality evidence. `AI_EVALUATION=NOT_RUN`, `BROWSER_E2E=NOT_RUN`, and `G3=PARTIAL`. Official rubric and representative group work remain `OPEN`.

WEB-01 can use [FRONTEND-HANDOFF.md](FRONTEND-HANDOFF.md) for component work. Browser-level reconnect, keyboard, responsive and readability checks remain separate frontend work and do not weaken the backend API verdict.
