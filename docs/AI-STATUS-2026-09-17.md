# AI backend status — 2026-09-17

## Current verdict

`AI-00_OFFLINE_SLICE_IMPLEMENTED / MAIN_VERIFIED / LIVE_GATE_OPEN`: the user approved separating offline
implementation from live provider readiness and will supply an API key later. The
application offers disabled/`DEV_FIXTURE_PLANNER` modes, not this AI adapter. No live model,
embedding provider, pgvector latency/quality measurement, query expansion, or
local replan result is claimed. Offline exact retrieval, isolated pgvector and
fake planner tests are implementation evidence only.

AI-00 is a design and evidence gate, not an AI quality pass. Its output is the
[AI backend design](superpowers/specs/2026-09-17-ai-backend-design.md) and the
[implementation plan](superpowers/plans/2026-09-17-ai-backend.md).

## Verified starting point

- `PlannerPort` is the engine seam; the engine parses `PlannerResultSchema`,
  validates the returned plan against the reviewed gateway, performs the
  read-only preview and creates the approval snapshot.
- `DEV_FIXTURE_PLANNER` is server-owned and accepts only the reviewed fixture
  prompts. It is lifecycle evidence, not AI evidence.
- Prompt builders already define planning, bounded repair, query expansion and
  local-replan language. Prompt text does not replace engine validation or
  policy checks.
- The reviewed runtime catalog is 8 `task_hub` tools plus 2 optional
  `filesystem` tools. Retrieval must consume this reviewed snapshot, never raw
  MCP annotations or arbitrary client-supplied launch data.
- Migration `0007` adds a separate `reviewed_*` catalog/index store: it never
  queries the historical raw `tools` table. A complete 8+2 snapshot plus
  matching provider/model/dimensions/preprocessing/content/catalog hashes is
  required before atomic activation. The current API still does not populate or
  query that adapter.

## Gate state

| Gate | Status | Meaning |
|---|---|---|
| AI-00 offline design gate | `IMPLEMENTED_MAIN_VERIFIED` | Catalog/ports/exact retrieval/fake planner exist; foundation review was unavailable; planner hardening has a scoped independent review |
| AI-00 live readiness gate | `OPEN` | Account/schema probe and numeric budgets still required; overall AI-00 not fully closed |
| AI-01 semantic retrieval + query expansion | `PARTIAL_MAIN_VERIFIED` | Exact pgvector adapter, provenance and isolated two-gateway/DB tests exist; query expansion, API wiring and measurement remain open |
| AI-02 provider planner + bounded repair | `NOT_RUN` | Terra selected by user; backend API ID/access/schema not probed |
| AI-03 local replan | `NOT_RUN` | Contract exists; runtime is not wired |
| AI-04 evaluation and holdout | `NOT_RUN` | No model result, cost, latency or correctness result |
| Overall AI verdict | `NOT_RUN` | Must not be inferred from fixture/API passes |

## Confirmed decisions — 2026-09-17

Offline planner hardening verification: `npm run check` exit 0 (DSL 43, engine 134 +
1 skipped, API 26, web 38). After the final validator-exception fix, engine
build and full unit suite passed again: 135 passed, 1 skipped, including 16
planner tests. `git diff --check` passed. These are offline checks, not a live
provider or PostgreSQL integration run. No commit/push was made.

AI-01 has now added a seventh SQL migration and isolated PostgreSQL/pgvector
tests: full reviewed 8+2 gateway snapshot → ten synthetic 1536-dimension
vectors → exact cosine retrieval; a separate two-tool test verifies atomic
replacement and concurrent activation leaves exactly one active index. This is
database integration evidence, not an OpenAI embedding call, semantic-quality
measurement, API integration or p95 claim.

Latest local verification after AI-01: root typecheck and build exit 0; DSL 43,
engine unit 140 passed + 1 skipped, API unit 26 and web unit 38 passed. Engine
integration ran 65 passed tests against disposable `engine_it_*` databases;
the reviewed 8+2 → ten-vector exact-cosine test and pgvector activation race
test are included. `git diff --check` passed. No shared demo database, provider
or paid API was used; no commit/push was made.

- OpenAI / GPT-5.6 Terra is the user's chosen target, not verified API access.
- `text-embedding-3-large`, explicit 1536 dimensions, exact cosine over the
  reviewed catalog; comparative ATI quality remains NOT_RUN.
- Backend runtime environment injection, hidden local prompt by default;
  encrypted vault optional, no mandatory SecretStore dependency.
- PostgreSQL outbox + single sequential worker retained; BullMQ/Redis deferred.
  Existing Redis compose service is unchanged, not removed or made optional yet.

## Open inputs and next gate

- Public model ID `gpt-5.6-terra`, advertised Structured Outputs/medium reasoning
  and pricing reference are DOCS_VERIFIED on 2026-09-17 (source in design §6).
  Account access, exact ATI schema/settings probe and numeric
  deadline/retry/rate/cost budgets remain OPEN.
- Safe credential delivery and authorization for a bounded paid API probe;
  launcher and secret-isolation/redaction tests are not implemented yet.
- The official rubric and a representative group task remain external inputs
  needed for final G1 interpretation.
- Offline planner hardening now covers a shared retrieval/repair deadline,
  non-cooperative-client cancellation, preserved repair context, real engine
  validation and optional per-model-call metadata. Evidence is callback-only,
  not durable DB storage; provider adapters and API wiring remain outstanding.
  The default offline deadline is 60 seconds, not an approved live cost budget.
- Complete provider evidence before live integration/evaluation, not before
  offline work. A documentation or fake-client review cannot close the live gate.
